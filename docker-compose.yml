# docker-compose.yml

version: '3.8'
services:
  postgres:
    image: postgres
    environment:
      POSTGRES_USER: alto_user
      POSTGRES_PASSWORD: alto_password
      POSTGRES_DB: alto_db
    ports:
      - '5432:5432'

